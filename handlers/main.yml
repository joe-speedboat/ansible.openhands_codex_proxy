---
- name: restart nginx
  ansible.builtin.systemd:
    name: nginx
    state: restarted

- name: restart openhands compose stack
  ansible.builtin.command:
    cmd: "docker compose -f {{ openhands_codex_proxy_project_dir }}/docker-compose.yml restart"
  changed_when: true
...
